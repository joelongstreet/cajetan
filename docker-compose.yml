version: "3"
services:
  db:
    image: "postgres:11"
    container_name: "longshot"
    ports:
      - "54320:5432"
    volumes:
      - longshot:/var/lib/postgresql/data
    environment:
      - POSTGRES_HOST_AUTH_METHOD=trust
volumes:
  longshot: